default:
  image: node:14
  before_script:
    - apt update
    - yarn install

services:
  - 4tqrgqe5yrgfd/electron:latest

stages:
  - deploy

cache:
  paths:
    - node_modules/

deploy:
  script:
    - apt -y install rpm
    - yarn electron:build
  artifacts:
    paths:
      - dist_electron/*.AppImage
      - dist_electron/*.snap
      - dist_electron/*.deb
      - dist_electron/*.rpm
